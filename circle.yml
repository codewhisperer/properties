machine:
  node:
    version: 0.10.22
deployment:
  staging:
    branch: master
    commands:
      - npm run bundle
      - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
      - git push --force git@heroku.com:properties-app.git $CIRCLE_SHA1:refs/heads/master
